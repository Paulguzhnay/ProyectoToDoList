{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/doList-version1"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server/index.server.html"
    }
  ]
}
